<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>汽车行驶动画</title>
    <style>
        /* 页面基础样式 */
        body {
            margin: 0;
            overflow: hidden; /* 隐藏滚动条 */
            background: #f0f0f0;
        }

        /* 汽车容器 */
        .car {
            position: absolute;
            /* 初始位置：屏幕右上角 */
            top: 0;
            right: 0;
            /* 初始尺寸 */
            width: 50px;
            height: 30px;
            background-color: #ff4444;
            border-radius: 5px;
            /* 车头方向：初始旋转约-45度，指向左下角 */
            transform: rotate(-45deg);
            /* 应用动画 */
            animation: drive 8s ease-in-out forwards;
        }

        /* 汽车轮子（简单装饰） */
        .car::before, .car::after {
            content: '';
            position: absolute;
            width: 10px;
            height: 10px;
            background: #333;
            border-radius: 50%;
            bottom: -3px;
        }
        .car::before { left: 5px; }
        .car::after { right: 5px; }

        /* 关键帧动画：从右上角到左下角，同时放大 */
        @keyframes drive {
            0% {
                top: 0;
                right: 0;
                width: 50px;
                height: 30px;
                transform: rotate(-45deg);
            }
            100% {
                top: calc(100vh - 200px); /* 底部留出空间 */
                left: 0;
                width: 200px;
                height: 120px;
                transform: rotate(-45deg); /* 保持车头方向 */
            }
        }
    </style>
</head>
<body>
    <div class="car"></div>
</body>
</html>
